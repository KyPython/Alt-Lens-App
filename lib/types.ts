export interface MediaMetadata {
  mediaId: string;
  altText: string;
  caption?: string;
  userId: string;
  mediaUrl?: string;
  imageUrl?:string;
}

// You can add other shared types here in the future.
